#ifndef KomputerH
#define KomputerH
#include "rozgrywka.h"
   class Komputer
   {
        char poziom;
        int znajdz_najlepszy_ruch(Rozgrywka, int);
        public:
        void ustaw_poziom(char);
        void wykonaj_ruch(Rozgrywka&, Tokno*);
        Komputer();
   };

#endif
